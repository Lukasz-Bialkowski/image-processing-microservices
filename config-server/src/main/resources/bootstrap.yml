server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: /home/lukas/Software/master-thesis-microservice/config-server/src/main/resources/config-repo

eureka:
  client:
    region: default
    registryFetchIntervalSeconds: 5
    serviceUrl:
      defaultZone: ${EUREKA_SERVER_URI:http://localhost:8761/eureka/}

#      discovery:
#        enabled: true
#        service-id: config-server


#eureka:
#  client:
#    serviceUrl:
#      defaultZone: ${EUREKA_SERVER_URI:http://localhost:8761/eureka/}
#      defaultZone: http://eureka-server:8761/eureka/
