spring.application.name=worker-service
server.port=8090

# Discovery first
spring.cloud.config.name=worker-service
spring.cloud.config.discovery.enabled=true
spring.cloud.config.discovery.service-id=config-server

eureka_server_host=localhost
eureka_server_port=8761
eureka.client.serviceUrl.defaultZone=http://${eureka_server_host}:${eureka_server_port}/eureka/

# Config first
#spring.cloud.config.uri=http://${config_server_host}:${config_server_port}