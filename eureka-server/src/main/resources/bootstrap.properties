# Property is needed in a bootstrap phase
# Used to find proper properties file in configuration repository e.g. 'eureka-server.properties'
# Changes applied in this file are overriden by those in configuration repository
spring.application.name=eureka-server