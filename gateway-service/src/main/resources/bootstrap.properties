spring.application.name=gateway-service
server.port=8080

# Discovery first
spring.cloud.config.name=gateway-service
spring.cloud.config.discovery.enabled=true
spring.cloud.config.discovery.service-id=config-server

eureka_server_host=localhost
eureka_server_port=8761
eureka.client.serviceUrl.defaultZone=http://${eureka_server_host}:${eureka_port}/eureka/

# Config first
#spring.cloud.config.uri=${CONFIG_SERVER_URI:http://localhost:8888}